# AWS
AWS_REGION=us-west-2
S3_BUCKET=your-bucket-name
# Provide credentials in environment variables or via IAM role
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...

# Gemini (optional) - if not provided, server returns mock metadata
GEMINI_API_KEY=

# JWT secret for auth (optional)
JWT_SECRET=

# Server port
PORT=4000
